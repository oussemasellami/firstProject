{% extends 'base.html.twig' %}

{% block title %}Student{% endblock %}

{% block body %}
    <h1>Student</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ student.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ student.username }}</td>
            </tr>
            <tr>
                <th>Moyenne</th>
                <td>{{ student.moyenne }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_studentt_index') }}">back to list</a>

    <a href="{{ path('app_studentt_edit', {'id': student.id}) }}">edit</a>

    {{ include('studentt/_delete_form.html.twig') }}
{% endblock %}
